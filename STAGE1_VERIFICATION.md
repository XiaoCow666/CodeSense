# 阶段一验证说明

本文记录当前 `main` 中“阶段一：思路描述”的可复现入口和验证边界。内容依据
`routes/thinking.py`、`utils/thinking_ai.py` 和
`tests/test_stage1_evaluation.py` 的现有实现整理，不扩展接口，也不替代部署文档。

## 本地验证

在项目根目录执行：

```powershell
python -m pip install -r requirements-test.txt
python -m pytest tests/test_stage1_evaluation.py -q
```

这两个测试分别验证：

- 结构化的“问题 / 回答”输入可以走本地快速评判，不调用远程 AI；
- 非结构化输入仍保留 AI 评判兜底，并能解析评分和评语。

测试使用替身对象隔离远程服务，因此不需要 API 密钥，也不会证明真实 AI provider 的可用性。

## HTTP 入口

入口为 `POST /thinking/api/stage1/submit`。调用前需要：

1. 先登录，接口受 `login_required` 保护；
2. 创建属于当前学生的学习会话；
3. 为该会话对应的作业准备思维预设（关键步骤）。

请求体的最小形状为：

```json
{
  "session_id": 123,
  "description": "我会先读取输入，再按关键步骤处理并输出结果。"
}
```

其中 `session_id` 必须替换为真实学习会话 ID。`description` 会先去除首尾空白，长度少于 5 个字符时返回 HTTP 400；会话不存在或不属于当前学生时返回 HTTP 403；找不到思维预设时返回 HTTP 500。

成功响应包含以下字段：

```json
{
  "success": true,
  "score": 63.0,
  "feedback": "方向基本正确",
  "passed": true
}
```

评分达到 50 分时，当前学习会话进入阶段二；未达到时，接口仍返回评估结果，但不会记录本次阶段通过事件。

## 事实与推断边界

已由当前源码或测试直接证实的事实：

- 阶段一评估结果是 0–100 范围的分数和一段评语；
- 结构化回答优先尝试本地快速评判；无法可靠判定时才创建 AI 客户端；
- 阶段一接口会把描述和评估结果写入学习会话，并记录提交事件；
- 通过阈值为 `score >= 50`。

本说明不声称已经证实以下行为：

- 真实 AI provider 的网络连通性、限流、计费或返回质量；
- 在浏览器中完成登录、创建会话并提交阶段一的完整端到端流程；
- SSE 流式响应、数据库迁移以及生产部署环境中的行为。

这些场景需要分别配置外部服务或运行更高层级的集成测试，本次文档改动不改变它们。
