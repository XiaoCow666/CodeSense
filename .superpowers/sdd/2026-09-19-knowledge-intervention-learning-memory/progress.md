# SDD ledger — plan: docs/superpowers/plans/2026-09-19-knowledge-intervention-learning-memory.md

## 计划冲突检查

| 范围 | 关系 | 检查结果 | Ruling |
| --- | --- | --- | --- |
| 任务一 / 任务二 | `list_student_learning_sources` 的摘要被首页模板消费 | 接口先在服务层确定，首页只接收有界投影 | Ruling: 保持来源摘要与 snapshot 分离，避免首页读取向量正文；错误代价是模板传参增加一个字段，收益是隐私边界清晰 |
| 任务一 / 任务三 | 撤回状态与陈旧状态都影响召回 | 撤回使用行状态，陈旧使用索引状态，状态含义可独立测试 | Ruling: 召回前同时检查索引新鲜度与来源状态，避免重建自动恢复本人撤回 |
| 任务四 / 任务五 | 教师知识点页与学生 AI 图谱提示共同使用 `learning_graph` | 教师使用班级聚合，学生使用当前作业个人投影 | Ruling: 两条路径分别执行角色过滤，禁止复用教师聚合数据到学生提示；错误代价是代码有少量重复，收益是权限边界明确 |
| 任务四 / 任务六 | 教师动作链接需要继续使用现有布置入口 | 既有 `/assign/<assignment_id>` 已有作业所有权检查 | Ruling: 新页面只提供导航，不复制班级分配写入逻辑，降低状态分叉风险 |
| 任务五 / 任务六 | AI 响应新增上下文字段可能影响旧客户端 | 只增加 `data` 内字段，SSE `done` 保留旧字段和顺序 | Ruling: 采用增量字段并测试旧字段存在，错误代价是响应略有增大，收益是客户端兼容 |
| 任务七 / 全部任务 | 发布资料依赖部署门禁 | 工作区实现与生产发布分离 | Ruling: 生产连接、GitHub Release、飞书和知识库逐项核验，任一连接缺失时保留候选并记录 `needs_human` |

## 任务自洽检查

| 任务 | 文件与测试 | 结果 |
| --- | --- | --- |
| 任务一 | `student_vector_store.py`、`routes/main.py`、`test_student_vector_store.py` | 接口和测试名称一致 |
| 任务二 | `student_learning_memory.html`、`modern.css`、首页测试 | 页面消费来源摘要，状态来自服务投影 |
| 任务三 | 向量服务、离线 fixture、评测服务 | 陈旧状态不要求数据库字段迁移 |
| 任务四 | `learning_graph.py`、`routes/main.py`、教师模板、图谱测试 | 教师页面只消费班级聚合作业 |
| 任务五 | `learning_graph.py`、`routes/api.py`、AI 测试 | 当前作业作为唯一图谱范围 |
| 任务六 | 真实测试客户端、运行报告 | 覆盖学生、教师、管理员与返回状态 |
| 任务七 | README、CHANGELOG、信息图、运行报告 | 版本资料只在部署核验后进入发布闭环 |

## Rulings

- Ruling: 本轮采用无数据库字段新增的陈旧判定，依据是现有 `last_built_at` 已能表达索引生命周期；错误代价是来源级时间策略暂缓，后续可在有数据样本后补充。
- Ruling: 教师知识点页复用已有布置页面，依据是现有写入流程已经含作业所有权和班级权限；错误代价是页面动作粒度保持现状，收益是避免重复写权限逻辑。

## 状态

- 2026-09-19：隔离工作树已建立，候选基线全量测试 `817 passed`。
- 2026-09-19：失败测试已写入，目标测试因缺少新接口在收集阶段失败。
- 2026-09-19：任务一至任务六均已完成，学生来源治理、陈旧索引恢复、教师知识点行动、图谱 AI 上下文和教师建议作业动作均有入口与回归。
- 2026-09-19：只读审查发现五项主流程问题，已修复并用定向测试复核；图谱独立降级、Code Studio 分析上下文、教师 SSE 作业动作、布置权限显示和撤回存在性隐私均已覆盖。
- 2026-09-19：最新远端 `d3673e7` 已快进合并；全量测试 `829 passed, 2183 warnings`，compileall 与 `git diff --check` 通过。
- 2026-09-19：版本材料与信息图已生成，生产 Workbench、部署、Release、飞书和知识库步骤保持待门禁状态。
