# 密码找回与状态恢复集成候选

- 日期：2026-09-12（Asia/Shanghai）
- 决策：按用户确认保留密码找回、邮箱验证和学生登录功能，并与状态恢复/可访问性改进合并验证。
- 候选分支：`codex/status-password-deploy-20260912`
- 候选 HEAD：`c51464b`（已合并最新 `origin/main`）

## 纳入范围

密码找回与邮箱验证功能来自以下已审阅的聚焦提交，基于服务器当前基线 `5ee9367` 整理：

- `ae58001`：学生登录与密码找回
- `843c74a`：兼容旧部署的学号迁移
- `270280a`：邮箱注册验证

状态恢复与可访问性改进来自：

- `ee8ee9e`：状态恢复体验方案
- `334c4a8`：错误页、请求追踪、移动导航、反馈表单、AI 建议、提交评测状态等改进

## 验证结果

定向回归：`31 passed`。

全量回归：

```text
496 passed, 1559685 warnings in 343.98s (0:05:43)
```

辅助检查全部通过：

- Python `compileall`：通过
- `static/js/sse-client.js` Node 语法检查：通过
- `git diff --check`：通过

此前探索性全量运行曾出现一次提交评测 worker 的间歇性失败；单测和该测试文件单独运行均通过，随后完整回归通过，本轮最终结果为上述 496/496。该现象保留为后续测试隔离性观察项，不作为本候选的当前失败。

## 发布状态

本轮只在隔离候选工作树完成合并和验证，没有向服务器写入、推送或部署，也没有覆盖服务器上用户要求保留的未提交改动。当前结论为 `candidate_ready_local`：功能代码和回归证据已具备，生产发布前仍需单独确认 SMTP 配置、数据库表初始化/迁移及线上密码邮件闭环。
